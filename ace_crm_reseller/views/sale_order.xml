<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_action_quotations_new" model="ir.actions.act_window">
        <field name="name">Quotation</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">form,tree,graph</field>
        <field name="domain">[('reseller_op_id', '=', active_id)]</field>
        <field name="context">{'search_default_opportunity_id': active_id, 'default_reseller_op_id': active_id}</field>
    </record>

    <record id="sale_action_quotations" model="ir.actions.act_window">
        <field name="name">Quotations</field>
        <field name="res_model">sale.order</field>
        <field name="search_view_id" ref="sale.sale_order_view_search_inherit_quotation"/>
        <field name="domain">[('reseller_op_id', '=', active_id)]</field>
        <field name="context">{'search_default_reseller_op_id': [active_id], 'default_reseller_op_id': active_id}</field>
    </record>

    <record id="sale_action_orders" model="ir.actions.act_window">
        <field name="name">Sale orders</field>
        <field name="res_model">sale.order</field>
        <field name="domain">[('reseller_op_id', '=', active_id), ('state', 'not in', ('draft', 'sent', 'cancel'))]</field>
        <field name="context">{'search_default_reseller_op_id': [active_id], 'default_reseller_op_id': active_id}</field>
    </record>

    <record id="sale_view_inherit" model="ir.ui.view">
        <field name="name">sale_view_inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='technical']" position="inside">
                <field name="reseller_op_id" groups="base.group_no_one"/>
            </xpath>
            <xpath expr="//field[@name='partner_id']/.." position="inside">
                <field name="primary"/>
            </xpath>
        </field>
    </record>
</odoo>